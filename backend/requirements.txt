# ============================================================
# TYE (Track Your Expenses) — Backend Requirements
# ============================================================
# Install with:
#   python -m venv venv
#   venv\Scripts\activate        (Windows)
#   source venv/bin/activate     (Mac/Linux)
#   pip install -r requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20    # file uploads (OCR receipts)

# --- Database ---
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11     # PostgreSQL driver (pre-built binary, no GCC needed)

# --- Data Validation ---
pydantic==2.12.5

# --- Auth (Google OAuth) ---
google-auth==2.38.0

# --- HTTP Client (used by ocr_service.py) ---
requests==2.32.5

# --- Environment Variables ---
python-dotenv==1.0.1

# --- Testing ---
pytest==9.0.2
httpx==0.28.1               # required by FastAPI TestClient
